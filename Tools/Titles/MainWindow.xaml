<Window x:Class="Titles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Titles"
        xmlns:WPFDrawing="clr-namespace:WPF.Drawing;assembly=WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="2900" Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <GroupBox Header="Data" Name="DataFieldTags" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3">
            <Grid Background="LightGray">
                <Grid.RowDefinitions>
                    <RowDefinition Height="250" />
                    <RowDefinition Height="250" />
                    <RowDefinition Height="250" />
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="Episode Title" Margin="16,20,173,0" VerticalAlignment="Top" Width="49" Height="25" />
                <TextBox Grid.Row="0" Margin="16,50,10,19" Background="Azure" Text="{Binding TitleLabel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" />

                <Label Grid.Row="1" Content="Main Title" Margin="10,10,136,0" VerticalAlignment="Top" Height="25" />
                <TextBox Grid.Row="1" Margin="16,40,10,24" Background="Azure" Text="{Binding TitleLabelB, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" />

                <Button Grid.Row="2" Content="Save Image" Click="Button_Click" Height="32" Width="84" Margin="10,10,144,208" />
            </Grid>
        </GroupBox>
        <WPFDrawing:Outlined2Text Grid.Row="0" Grid.Column="1"
                x:Name="ShowTitle" Fill="{Binding FillColor}" Font="Russo One" FontSize="150"
                Bold="True"
                Text="{Binding TitleLabel}" Margin="5"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Stroke="Black"
                StrokeThickness="7"
                OuterStroke="White"
                OuterStrokeThickness="10" />
        <WPFDrawing:Outlined2Text Grid.Row="1" Grid.Column="1"
                x:Name="ShowTitleB" Fill="{Binding FillColor}" Font="Russo One" FontSize="100"
                Bold="True"
                Text="{Binding TitleLabelB}" Margin="5"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Stroke="Black"
                StrokeThickness="6"
                OuterStroke="White"
                OuterStrokeThickness="8" />
    </Grid>
</Window>